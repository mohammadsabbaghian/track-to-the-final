/* Vireo · The Track to the Final — shared sync configuration.
 *
 * Paste your Firebase WEB app config below, then commit this file.
 * Until real values are here the app runs in LOCAL-ONLY mode
 * (each device keeps its own picks; merge with Export / Import).
 *
 * These values are NOT secret — Firebase web config is meant to ship in
 * client code. Access is controlled by the Firestore rules (see README). */
window.FIREBASE_CONFIG = {
  apiKey: "PASTE_API_KEY",
  authDomain: "PASTE_PROJECT.firebaseapp.com",
  projectId: "PASTE_PROJECT",
  appId: "PASTE_APP_ID"
};
